letter_mapping.cpp can be compiled and run as a standalone program. It will as for 
integer input via the terminal, and print whether the number may be translated to a 
string. TIme and space complexity discussed inside. 